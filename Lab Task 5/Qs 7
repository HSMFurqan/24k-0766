#include <stdio.h>
int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    (number % 10 == 0) 
        ? printf("Last Digit is zero\n") 
        : printf("Last Digit non-zero\n");
    return 0;
}
