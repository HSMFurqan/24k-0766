#include <stdio.h>
int main() {
     int a ;
     for(a=0; a<=4; a++)
     {
         if(a%4==0)
         {printf("* * * * *\n");}
         else if (a==1 || a==3)
         {printf("  *   *\n");}
         else
         {printf("    *\n");}
     }
    return 0;
}
