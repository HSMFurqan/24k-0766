#include <stdio.h>
int multiply(int a, int b) {
    return a * b;
}
int main() {
    int num1, num2, product;
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);
    product = multiply(num1, num2);
    printf("Product: %d\n", product);
    return 0;
}
